# Mobile App

Flutter scaffold for the iOS and Android clients.

Current scope:

- browse public listings
- basic OTP auth flow
- seller draft creation and AI actions via REST
- mock checkout entry points

Flutter is not installed in the local execution environment used for this implementation, so this scaffold is provided and wired to the backend API, but not locally executed here.
